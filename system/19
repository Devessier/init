# Commande : `systemctl list-unit-files --type=service | grep enabled | awk -F '.service' '{print $1}'`

Sortie a titre d'exemple :

```
anacron
apparmor
autovt@
avahi-daemon
bluetooth
console-setup
cron
cups-browsed
cups
dbus-fi.w1.wpa_supplicant1
dbus-org.bluez
dbus-org.freedesktop.Avahi
dbus-org.freedesktop.timesync1
getty@
keyboard-setup
networking
rsyslog
ssh
sshd
syslog
systemd-fsck-root
systemd-timesyncd
wpa_supplicant
```

Ces services sont appelés `daemons`
