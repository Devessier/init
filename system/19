# Commande : `systemctl list-unit-files --type=service | grep enabled | awk -F '.service' '{print $1}'`

Sortie a titre d'exemple :

```
apparmor
autovt@
console-setup
cron
dbus-org.freedesktop.timesync1
getty@
keyboard-setup
networking
rsyslog
ssh
sshd
syslog
systemd-fsck-root
systemd-timesyncd
```

Ces services sont appeles `daemons`
